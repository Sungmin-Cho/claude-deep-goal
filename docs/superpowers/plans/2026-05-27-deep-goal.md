# deep-goal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자의 장기 작업 요청을 평가·재구성·컴파일하여 Claude Code / Codex의 네이티브 `/goal`에 붙여 넣을 완성된 조건을 제시하는 가벼운 cross-platform 플러그인을 만든다.

**Architecture:** 콘텐츠 위주 플러그인(코드 없음). 얇은 진입 스킬(`deep-goal`)이 코어 워크플로우 스킬(`deep-goal-workflow`)을 로드하고, workflow는 `references/`(rubric·compiler·platform-matrix·prep-scout)와 `references/recipes/`(시너지 레시피 3종)를 참조한다. 테스트는 형제 플러그인 방식대로 `scripts/verify-plugin.sh`(grep 기반 release-lint) + 매니페스트 JSON 파싱으로 수행한다.

**Tech Stack:** Markdown(SKILL/가이드/레시피), JSON(매니페스트), Bash(검증 스크립트, hermetic), Node(JSON 파싱 self-test). 런타임 의존성 0.

**참고 표본:** `/Users/sungmin/Dev/claude-plugins/deep-docs`(가장 가벼운 형제 — skill-based entry, 2단 스킬, verify-fixes.sh). 매니페스트·SKILL frontmatter·verify 스크립트 형식을 이 표본에서 차용한다.

**Spec:** `docs/superpowers/specs/2026-05-27-deep-goal-design.md` (모든 설계 결정·근거의 원천)

---

## File Structure

| 파일 | 책임 |
|---|---|
| `.claude-plugin/plugin.json` | Claude Code 매니페스트 |
| `.codex-plugin/plugin.json` | Codex 매니페스트 (`skills` + `interface`) |
| `package.json` | `type: module`, `verify` npm script |
| `scripts/verify-plugin.sh` | grep 기반 release-lint (테스트 스위트) |
| `skills/deep-goal/SKILL.md` | 얇은 사용자 진입, self-contained |
| `skills/deep-goal-workflow/SKILL.md` | 코어 6단계 워크플로우 |
| `skills/deep-goal-workflow/references/fitness-rubric.md` | 적합성 판정 기준 |
| `skills/deep-goal-workflow/references/condition-compiler.md` | 4요소 + 평가자 표면화 규칙 |
| `skills/deep-goal-workflow/references/platform-matrix.md` | Claude vs Codex 분기표 |
| `skills/deep-goal-workflow/references/prep-scout.md` | 사전 준비물 탐색 절차 |
| `skills/deep-goal-workflow/references/recipes/README.md` | 레시피 인덱스 + 플러그인 감지 규칙 |
| `skills/deep-goal-workflow/references/recipes/robust-implementation.md` | deep-work + deep-review 레시피 |
| `skills/deep-goal-workflow/references/recipes/autonomous-evolution.md` | deep-evolve 레시피 |
| `skills/deep-goal-workflow/references/recipes/ship-and-document.md` | deep-docs + deep-wiki 레시피 |
| `CLAUDE.md` / `AGENTS.md` | 플랫폼별 프로젝트 가이드 |
| `README.md` / `README.ko.md` | 이중언어 README |
| `CHANGELOG.md` / `CHANGELOG.ko.md` | 이중언어 CHANGELOG |

**비목표(생성하지 않음):** `agents/`, `hooks/`, 지속 상태/아티팩트/캐시 디렉토리. (spec §6, §13)

---

## Task 1: 매니페스트 + package.json

**Files:**
- Create: `.claude-plugin/plugin.json`
- Create: `.codex-plugin/plugin.json`
- Create: `package.json`

- [ ] **Step 1: `.claude-plugin/plugin.json` 작성**

```json
{
  "name": "deep-goal",
  "version": "1.0.0",
  "description": "Goal condition compiler — evaluates long-running task requests, reshapes them to fit, scouts prerequisites, and compiles ready-to-run native /goal conditions; skill-based entry (cross-platform: Claude Code / Codex / Copilot / Gemini)",
  "author": { "name": "Sungmin-Cho" },
  "repository": { "type": "git", "url": "https://github.com/Sungmin-Cho/claude-deep-goal.git" },
  "license": "MIT",
  "category": "Productivity",
  "keywords": ["goal", "long-running", "autonomous", "orchestration", "harness"]
}
```

- [ ] **Step 2: `.codex-plugin/plugin.json` 작성**

```json
{
  "name": "deep-goal",
  "version": "1.0.0",
  "description": "Goal condition compiler for long-running autonomous work",
  "author": { "name": "Sungmin-Cho" },
  "repository": "https://github.com/Sungmin-Cho/claude-deep-goal.git",
  "license": "MIT",
  "keywords": ["goal", "long-running", "autonomous", "orchestration", "harness"],
  "skills": "./skills/",
  "interface": {
    "displayName": "Deep Goal",
    "shortDescription": "Compile a request into a ready-to-run /goal condition",
    "longDescription": "Evaluate whether a long-running request fits the native goal feature, reshape it, scout prerequisites, and compile a platform-tailored /goal condition you paste to activate.",
    "developerName": "Sungmin-Cho",
    "category": "Productivity",
    "capabilities": ["Interactive", "Read"],
    "defaultPrompt": ["$deep-goal:deep-goal <your long-running task>"],
    "brandColor": "#4B5563",
    "screenshots": []
  }
}
```

- [ ] **Step 3: `package.json` 작성**

```json
{
  "name": "deep-goal",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Goal condition compiler for long-running autonomous work",
  "license": "MIT",
  "scripts": {
    "verify": "bash scripts/verify-plugin.sh"
  }
}
```

- [ ] **Step 4: 매니페스트 JSON 유효성 + 버전 일치 검증**

Run:
```bash
node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/plugin.json','utf8'))" && \
node -e "JSON.parse(require('fs').readFileSync('.codex-plugin/plugin.json','utf8'))" && \
node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" && \
echo "JSON OK" && \
for f in .claude-plugin/plugin.json .codex-plugin/plugin.json package.json; do node -e "console.log(JSON.parse(require('fs').readFileSync('$f','utf8')).version)"; done
```
Expected: `JSON OK` 후 `1.0.0`이 세 번 출력.

- [ ] **Step 5: Commit**

```bash
git add .claude-plugin .codex-plugin package.json
git commit -m "feat: deep-goal 매니페스트 + package.json 추가"
```

---

## Task 2: 검증 스크립트 (테스트 스위트)

**Files:**
- Create: `scripts/verify-plugin.sh`

이 스크립트가 이후 모든 콘텐츠 태스크의 "테스트"다. 지금 작성하면 아직 없는 파일들에 대한 체크가 FAIL한다 — 그게 의도된 failing 상태이며, 이후 태스크가 하나씩 GREEN으로 만든다.

- [ ] **Step 1: `scripts/verify-plugin.sh` 작성**

```bash
#!/usr/bin/env bash
# deep-goal release-lint — hermetic grep-based checks (no install needed).
set -u
PASS=0; FAIL=0
ok()   { PASS=$((PASS+1)); printf '  ✓ %s\n' "$1"; }
bad()  { FAIL=$((FAIL+1)); printf '  ✗ %s\n' "$1"; }
have() { [ -f "$1" ]; }
# assert file exists
af()   { if have "$1"; then ok "exists: $1"; else bad "missing: $1"; fi; }
# assert grep pattern present in file
ag()   { if have "$1" && grep -qE "$2" "$1"; then ok "$3"; else bad "$3"; fi; }
# assert pattern ABSENT across given files
an()   { if grep -rqE "$2" $1 2>/dev/null; then bad "$3"; else ok "$3"; fi; }

echo "== manifests =="
for f in .claude-plugin/plugin.json .codex-plugin/plugin.json package.json; do
  if have "$f" && node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" 2>/dev/null; then ok "valid json: $f"; else bad "invalid/missing json: $f"; fi
done
# version triple-sync
V1=$(node -e "console.log(JSON.parse(require('fs').readFileSync('.claude-plugin/plugin.json','utf8')).version)" 2>/dev/null)
V2=$(node -e "console.log(JSON.parse(require('fs').readFileSync('.codex-plugin/plugin.json','utf8')).version)" 2>/dev/null)
V3=$(node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).version)" 2>/dev/null)
if [ -n "$V1" ] && [ "$V1" = "$V2" ] && [ "$V1" = "$V3" ]; then ok "version sync ($V1)"; else bad "version mismatch: $V1/$V2/$V3"; fi
ag .codex-plugin/plugin.json '"skills"' "codex manifest declares skills"

echo "== required files =="
for f in \
  skills/deep-goal/SKILL.md \
  skills/deep-goal-workflow/SKILL.md \
  skills/deep-goal-workflow/references/fitness-rubric.md \
  skills/deep-goal-workflow/references/condition-compiler.md \
  skills/deep-goal-workflow/references/platform-matrix.md \
  skills/deep-goal-workflow/references/prep-scout.md \
  skills/deep-goal-workflow/references/recipes/README.md \
  skills/deep-goal-workflow/references/recipes/robust-implementation.md \
  skills/deep-goal-workflow/references/recipes/autonomous-evolution.md \
  skills/deep-goal-workflow/references/recipes/ship-and-document.md \
  CLAUDE.md AGENTS.md README.md README.ko.md CHANGELOG.md CHANGELOG.ko.md; do
  af "$f"
done

echo "== skill frontmatter =="
ag skills/deep-goal/SKILL.md '^name: deep-goal$' "entry skill: name"
ag skills/deep-goal/SKILL.md '^user-invocable: true$' "entry skill: user-invocable true"
ag skills/deep-goal/SKILL.md '^description:' "entry skill: description"
ag skills/deep-goal-workflow/SKILL.md '^name: deep-goal-workflow$' "workflow skill: name"
ag skills/deep-goal-workflow/SKILL.md '^user-invocable: false$' "workflow skill: user-invocable false"

echo "== core content invariants =="
# self-contained entry skill must inline the 4 compile elements + platform branch
ag skills/deep-goal/SKILL.md '종료(상태|조건)' "entry skill inlines end-state element"
ag skills/deep-goal/SKILL.md '(Codex|codex)' "entry skill mentions Codex branch"
ag skills/deep-goal/SKILL.md 'Skill\(' "entry skill documents cross-platform Skill() entry"
# evaluator surfacing rule must appear in compiler
ag skills/deep-goal-workflow/references/condition-compiler.md '(표면화|surface|대화에 (보고|명시))' "compiler states evaluator-surfacing rule"
ag skills/deep-goal-workflow/references/condition-compiler.md '4,?000' "compiler states 4000-char limit"
# rubric three verdicts
ag skills/deep-goal-workflow/references/fitness-rubric.md '(반려|부적합)' "rubric has reject verdict"
ag skills/deep-goal-workflow/references/fitness-rubric.md '재구성' "rubric has reshape verdict"
# recipes index lists all three recipes
ag skills/deep-goal-workflow/references/recipes/README.md 'robust-implementation' "recipes index lists robust-implementation"
ag skills/deep-goal-workflow/references/recipes/README.md 'autonomous-evolution' "recipes index lists autonomous-evolution"
ag skills/deep-goal-workflow/references/recipes/README.md 'ship-and-document' "recipes index lists ship-and-document"
# each recipe has trigger + termination sections
for r in robust-implementation autonomous-evolution ship-and-document; do
  ag "skills/deep-goal-workflow/references/recipes/$r.md" '(트리거|Trigger|감지)' "recipe $r: trigger section"
  ag "skills/deep-goal-workflow/references/recipes/$r.md" '(종료조건|종료 조건|Termination)' "recipe $r: termination section"
done
# robust-implementation must disclose deep-work approval-gate caveat (spec §11)
ag skills/deep-goal-workflow/references/recipes/robust-implementation.md '(Exit Gate|승인 게이트|승인)' "robust recipe discloses deep-work approval gate"

echo "== non-goals enforced =="
# v1 must NOT ship hooks/ or agents/ dirs
if [ -d hooks ]; then bad "hooks/ dir present (v1 non-goal)"; else ok "no hooks/ dir"; fi
if [ -d agents ]; then bad "agents/ dir present (v1 non-goal)"; else ok "no agents/ dir"; fi

echo "== placeholders =="
an "skills CLAUDE.md AGENTS.md README.md README.ko.md" '(TBD|FIXME|TODO|작성 예정|fill in)' "no placeholder tokens in shipped content"

echo ""
echo "Passed: $PASS, Failed: $FAIL"
[ "$FAIL" -eq 0 ]
```

- [ ] **Step 2: 스크립트 실행 권한 부여 + 실행 (failing 확인)**

Run:
```bash
chmod +x scripts/verify-plugin.sh && bash scripts/verify-plugin.sh; echo "exit=$?"
```
Expected: 매니페스트 체크는 `✓`, 아직 없는 콘텐츠 파일들은 `✗ missing: ...`. 마지막 줄 `Passed: N, Failed: M` (M>0), `exit=1`. **이것이 의도된 failing 테스트 상태.**

- [ ] **Step 3: Commit**

```bash
git add scripts/verify-plugin.sh
git commit -m "test: deep-goal release-lint 검증 스크립트 추가 (테스트 스위트)"
```

---

## Task 3: 코어 워크플로우 스킬 (deep-goal-workflow)

**Files:**
- Create: `skills/deep-goal-workflow/SKILL.md`

- [ ] **Step 1: `skills/deep-goal-workflow/SKILL.md` 작성**

frontmatter:
```markdown
---
name: deep-goal-workflow
description: deep-goal 코어 워크플로우 — 감지/적합성평가/재구성/레시피매칭/사전준비물탐색/컴파일제시 6단계와 references 정의. deep-goal 진입 스킬이 자동 로드한다.
user-invocable: false
---
```

본문에 반드시 포함할 섹션(spec §8 충실 반영):
1. **개요** — deep-goal의 정체성 한 줄(메타-Guide), 활성화는 네이티브 `/goal` 제시 + 사용자 트리거임을 명시(자동 호출 불가, spec §3).
2. **References 로드 규칙** — `fitness-rubric.md`, `condition-compiler.md`, `platform-matrix.md`, `prep-scout.md`, `recipes/`를 description 매칭으로 로드. Codex 등에서 자동 로드가 약하면 `${CLAUDE_PLUGIN_ROOT}/skills/deep-goal-workflow/references/<파일>` Read fallback.
3. **6단계 절차** (각 단계 = 소제목):
   - ① 감지: 요청 파싱 / 플랫폼(Claude·Codex) / git / 설치된 deep-* 플러그인 감지(아래 §감지 방법)
   - ② 적합성 평가: `fitness-rubric` 적용
   - ③ 재구성 대화: 균형 게이트 (분해·종료조건 보강 / 구조적 부적합 시 반려 + 대안 `/loop`·일반 작업)
   - ④ 레시피 매칭: `recipes/README.md`의 감지 규칙으로 적용 가능 레시피 제안, 없으면 단발 goal
   - ⑤ 사전 준비물 탐색: `prep-scout` 절차로 읽을 파일·증명 커맨드·불변 제약 발굴
   - ⑥ 컴파일 + 제시: `condition-compiler` + `platform-matrix`로 플랫폼 맞춤 `/goal` 조건 생성, 복잡하면 PLAN.md 분리, 복사용 코드블록 + 근거 + 활성화 안내
4. **감지 방법** (spec §14 결정):
   - 플랫폼: 현재 런타임이 자기 자신을 안다(Claude Code / Codex). 사용자가 "반대 플랫폼용도 달라"면 양쪽 제시.
   - 설치된 deep-* 플러그인: 사용 가능한 스킬/슬래시 목록에서 `deep-work`, `deep-review`(`deep-review-loop`), `deep-evolve`, `deep-docs`, `deep-wiki` 존재 확인. 불확실하면 사용자에게 1회 확인.
5. **활성화 안내 문구 템플릿** — "아래 한 줄을 그대로 입력/붙여넣어 활성화하세요" + 코드블록.

- [ ] **Step 2: frontmatter·구조 검증**

Run:
```bash
bash scripts/verify-plugin.sh 2>&1 | grep -E "workflow skill|exists: skills/deep-goal-workflow/SKILL.md"
```
Expected: `✓ exists: skills/deep-goal-workflow/SKILL.md`, `✓ workflow skill: name`, `✓ workflow skill: user-invocable false`.

- [ ] **Step 3: Commit**

```bash
git add skills/deep-goal-workflow/SKILL.md
git commit -m "feat: deep-goal-workflow 코어 6단계 스킬 추가"
```

---

## Task 4: References 4종

**Files:**
- Create: `skills/deep-goal-workflow/references/fitness-rubric.md`
- Create: `skills/deep-goal-workflow/references/condition-compiler.md`
- Create: `skills/deep-goal-workflow/references/platform-matrix.md`
- Create: `skills/deep-goal-workflow/references/prep-scout.md`

- [ ] **Step 1: `fitness-rubric.md` 작성**

spec §9 표를 본문으로. 반드시 포함:
- ✅ 적합 신호: 단일 목표 / 검증 가능한 종료조건 / "한 프롬프트보다 크고 백로그보다 작음" / 진행 증명 루프 존재
- 🔧 **재구성** 신호 + 대응 (종료조건 모호→측정가능화, 범위과대→분해, 증명부재→커맨드 식별)
- ⛔ **반려** 신호 + 대안 (검증 불가 주관 목표 / 단발성 / 무관한 잡다 목록 → `/loop`·일반 작업)
- 각 판정의 구체 예시 2개 이상.

- [ ] **Step 2: `condition-compiler.md` 작성**

반드시 포함:
- **4요소**: ① 측정 가능한 종료상태 ② 증명 방법(커맨드/아티팩트) ③ 불변 제약 ④ 상한(턴/시간)
- **평가자 표면화 규칙**(핵심): Claude 평가자는 도구를 못 쓰고 대화에 **표면화**된 출력만 본다 → 조건에 "각 단계/게이트 결과를 대화에 명시 보고하라"는 지침을 반드시 넣는다.
- **4,000자 한도**와 초과 시 PLAN.md 분리 전략.
- 단발 goal 예시 1개 + 레시피 기반 조건 예시 1개(spec §10의 robust-implementation 예시 그대로 사용).

- [ ] **Step 3: `platform-matrix.md` 작성**

spec §2 + §10 표를 본문으로. Claude 분기(평가자 제약·4000자·`or stop after N turns`)와 Codex 분기(contract 4항목·체크포인트·진행 로그·`pause`/`resume`·PLAN.md) 대비표 + 각 플랫폼 컴파일 예시.

- [ ] **Step 4: `prep-scout.md` 작성**

사전 준비물 탐색 절차(메인 인라인, 서브에이전트 없음): Glob/Grep/Read로 (1) 먼저 읽을 파일 (2) 진행 증명 커맨드(테스트/빌드/lint 추정 — `package.json` scripts, Makefile, CI 설정 등에서) (3) 불변 제약을 발굴하는 단계별 절차. git 여부에 따른 분기.

- [ ] **Step 5: 검증**

Run:
```bash
bash scripts/verify-plugin.sh 2>&1 | grep -E "compiler states|rubric has|exists: skills/deep-goal-workflow/references/(fitness|condition|platform|prep)"
```
Expected: 6개 항목 모두 `✓` (4파일 존재 + compiler 표면화/4000 + rubric 재구성/반려).

- [ ] **Step 6: Commit**

```bash
git add skills/deep-goal-workflow/references/fitness-rubric.md skills/deep-goal-workflow/references/condition-compiler.md skills/deep-goal-workflow/references/platform-matrix.md skills/deep-goal-workflow/references/prep-scout.md
git commit -m "feat: deep-goal references 4종 (rubric/compiler/platform/prep-scout) 추가"
```

---

## Task 5: 시너지 레시피 라이브러리

**Files:**
- Create: `skills/deep-goal-workflow/references/recipes/README.md`
- Create: `skills/deep-goal-workflow/references/recipes/robust-implementation.md`
- Create: `skills/deep-goal-workflow/references/recipes/autonomous-evolution.md`
- Create: `skills/deep-goal-workflow/references/recipes/ship-and-document.md`

각 레시피 문서 공통 섹션: **트리거(감지 조건)** / **전제(필요 플러그인)** / **시퀀스** / **종료조건** / **컴파일된 `/goal` 예시(Claude·Codex)** / **주의(현실적 제약)**.

- [ ] **Step 1: `recipes/README.md` 작성**

- 레시피 인덱스(3개 링크 + 한 줄 설명).
- **플러그인 감지 규칙**: 어떤 플러그인이 감지되면 어떤 레시피를 제안하는지 매핑 표.
- 복수 레시피 적용 가능 시 사용자에게 선택받는 규칙.
- 아무 레시피도 매칭 안 되면 단발 goal로 폴백.

- [ ] **Step 2: `robust-implementation.md` 작성**

- 트리거: `deep-work` AND `deep-review`(또는 `deep-review-loop`) 감지.
- 시퀀스: deep-work Research→Plan→Implement→Test, Plan 승인 직후·Implement 완료 직후 `deep-review-loop(--max=3)` APPROVE까지.
- 종료조건: 모든 phase 완료 AND 최종 review-loop APPROVE AND 테스트 전체 통과.
- 컴파일 예시(Claude): spec §10 예시 그대로 — 마지막에 "각 단계 결과를 대화에 명시 보고" 문구 포함.
- **주의(필수)**: deep-work는 phase마다 Exit Gate(AskUserQuestion 승인) + Research/Plan 2차 승인이 있어 **완전 무인 자율 불가**. goal은 턴 간 프롬프트만 제거하고 승인 지점은 사용자 입력을 요구함을 정직하게 안내. (spec §11)

- [ ] **Step 3: `autonomous-evolution.md` 작성**

- 트리거: `deep-evolve` 감지.
- 시퀀스: deep-evolve 실험 루프를 fitness metric 목표치까지 반복.
- 종료조건: fitness metric ≥ 목표치 OR 상한 도달. metric 값을 대화에 보고.
- 컴파일 예시(Claude·Codex).
- 주의: deep-evolve의 자체 측정 루프와 네이티브 goal 상한의 관계.

- [ ] **Step 4: `ship-and-document.md` 작성**

- 트리거: `deep-docs` AND `deep-wiki` 감지(deep-review 있으면 최종 게이트 추가).
- 시퀀스: 구현 완료 가정 → `deep-docs garden` → `wiki-ingest` → (있으면) 최종 `deep-review`.
- 종료조건: docs 정비 완료 AND wiki 반영 완료 AND (있으면) review APPROVE. 각 결과 대화 보고.
- 컴파일 예시(Claude·Codex).

- [ ] **Step 5: 검증**

Run:
```bash
bash scripts/verify-plugin.sh 2>&1 | grep -E "recipes index|recipe (robust|autonomous|ship)|robust recipe discloses"
```
Expected: recipes index 3개 + 각 레시피 trigger/termination + robust 승인게이트 고지 모두 `✓`.

- [ ] **Step 6: Commit**

```bash
git add skills/deep-goal-workflow/references/recipes
git commit -m "feat: 시너지 레시피 3종 + 감지 규칙 추가"
```

---

## Task 6: 진입 스킬 (deep-goal, self-contained)

**Files:**
- Create: `skills/deep-goal/SKILL.md`

deep-docs 진입 SKILL의 self-containment 패턴을 따른다 — 타 플랫폼에서 sibling 자동 로드가 약해도 동작하도록 workflow 핵심을 **인라인 보존**(의도적 duplication).

- [ ] **Step 1: `skills/deep-goal/SKILL.md` 작성**

frontmatter:
```markdown
---
name: deep-goal
description: Use when the user wants to turn a long-running task into a native /goal condition — evaluating fit, reshaping, scouting prerequisites, and compiling a ready-to-paste condition. Triggers on `/deep-goal`, "set a goal", "long-running task", "run until done", "goal 조건", "장기 자율 진행", "goal로 만들어줘". Compiles for Claude Code or Codex; suggests deep-suite synergy recipes when sibling plugins are installed.
user-invocable: true
---
```

본문 필수 요소:
1. **Invocation** — 두 경로: `/deep-goal <요청>`(Claude 슬래시) / `Skill({ skill: "deep-goal:deep-goal", args })`(타 플랫폼). 무인수 시 대화 진입.
2. **Prerequisites** — `deep-goal-workflow` 로드(description 매칭) + Codex fallback Read 경로. **self-containment 선언**: 아래 핵심 규칙은 의도적 duplication이며 변경 시 workflow와 동기화 필요.
3. **인라인 핵심**(self-contained):
   - 활성화 모델: 네이티브 `/goal`은 자동 호출 불가 → 완성 조건 제시 + 사용자 트리거 (spec §3).
   - 적합성 3판정 요약(적합/재구성/반려).
   - 컴파일 **4요소** + **평가자 표면화 규칙**(Claude 평가자는 표면화된 출력만 본다 → 단계 결과 대화 보고 지침 필수).
   - 플랫폼 분기 요약(Claude: 4,000자·`or stop after N turns` / Codex: contract·pause/resume).
4. **6단계 절차 요약** — 상세는 workflow 참조.
5. **무인수 진입** — AskUserQuestion: "무엇을 끝까지 진행하고 싶나요?"

- [ ] **Step 2: self-contained 마커 검증**

Run:
```bash
bash scripts/verify-plugin.sh 2>&1 | grep -E "entry skill"
```
Expected: `entry skill: name`, `user-invocable true`, `description`, `inlines end-state element`, `mentions Codex branch`, `documents cross-platform Skill() entry` 모두 `✓`.

- [ ] **Step 3: Commit**

```bash
git add skills/deep-goal/SKILL.md
git commit -m "feat: deep-goal 진입 스킬 추가 (self-contained)"
```

---

## Task 7: 프로젝트 가이드 (CLAUDE.md / AGENTS.md)

**Files:**
- Create: `CLAUDE.md`
- Create: `AGENTS.md`

deep-docs의 CLAUDE.md/AGENTS.md 형식을 따른다(짧게 유지, 상세는 CHANGELOG).

- [ ] **Step 1: `CLAUDE.md` 작성**

포함: 프로젝트 개요(메타-Guide 정체성), Directory Structure, Key Concepts(활성화 모델·4요소·평가자 표면화·레시피), Slash commands 표(`/deep-goal`), Tests(`npm run verify`), 🚨 릴리스 워크플로우(deep-suite 마켓플레이스 동기화 + 버전 3중 sync + CHANGELOG 양 언어), Related repositories.

- [ ] **Step 2: `AGENTS.md` 작성**

deep-docs AGENTS.md 형식: Codex Project Guide, Runtime Surfaces(매니페스트·스킬·스크립트 경로), Verification(`node -e JSON.parse`, `npm run verify`), 릴리스 후 deep-suite 동기화 안내.

- [ ] **Step 3: 검증**

Run:
```bash
bash scripts/verify-plugin.sh 2>&1 | grep -E "exists: (CLAUDE|AGENTS).md|no placeholder"
```
Expected: 두 파일 존재 `✓`, placeholder 없음 `✓`.

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md AGENTS.md
git commit -m "docs: CLAUDE.md / AGENTS.md 프로젝트 가이드 추가"
```

---

## Task 8: README + CHANGELOG (이중언어)

**Files:**
- Create: `README.md`
- Create: `README.ko.md`
- Create: `CHANGELOG.md`
- Create: `CHANGELOG.ko.md`

- [ ] **Step 1: `README.md` / `README.ko.md` 작성**

포함: 제목 + 한 줄 정의, 핵심 제약(자동 호출 불가 → 조건 제시), 설치(Claude `/plugin install ... @Sungmin-Cho-claude-deep-suite`, Codex marketplace mirror), 사용법(`/deep-goal <task>` / `$deep-goal:deep-goal "task"`), 6단계 흐름 요약, 시너지 레시피 3종 표, deep-suite 링크. 상단에 상호 언어 링크.

- [ ] **Step 2: `CHANGELOG.md` / `CHANGELOG.ko.md` 작성**

Keep a Changelog 형식. `## [1.0.0] - 2026-05-27` 항목에 첫 릴리스 내용(평가/재구성/컴파일/사전탐색 코어 + 레시피 3종 + cross-platform).

- [ ] **Step 3: 검증 (전체 lint GREEN 기대 직전 단계)**

Run:
```bash
bash scripts/verify-plugin.sh 2>&1 | grep -E "exists: (README|README.ko|CHANGELOG|CHANGELOG.ko)"
```
Expected: 4파일 존재 모두 `✓`.

- [ ] **Step 4: Commit**

```bash
git add README.md README.ko.md CHANGELOG.md CHANGELOG.ko.md
git commit -m "docs: 이중언어 README + CHANGELOG 추가"
```

---

## Task 9: 최종 검증 + 마무리

**Files:**
- Modify: 필요 시 직전 태스크 산출물 (lint 실패 항목 교정)

- [ ] **Step 1: 전체 release-lint 실행 (전부 GREEN)**

Run:
```bash
bash scripts/verify-plugin.sh; echo "exit=$?"
```
Expected: 모든 항목 `✓`, 마지막 줄 `Passed: N, Failed: 0`, `exit=0`. 실패 항목이 있으면 해당 산출물을 교정하고 재실행.

- [ ] **Step 2: 매니페스트 재파싱 self-test**

Run:
```bash
npm run verify
```
Expected: `Passed: N, Failed: 0` (exit 0).

- [ ] **Step 3: 진입 스킬 ↔ workflow 동기화 점검 (수동)**

`skills/deep-goal/SKILL.md`의 인라인 핵심(4요소·평가자 표면화·플랫폼 분기 요약)이 `skills/deep-goal-workflow/references/condition-compiler.md` 및 `platform-matrix.md`와 모순되지 않는지 두 파일을 나란히 읽고 확인. 불일치 시 교정 후 `bash scripts/verify-plugin.sh` 재실행.

- [ ] **Step 4: Commit (교정이 있었던 경우)**

```bash
git add -A
git commit -m "chore: deep-goal v1.0.0 release-lint GREEN — 최종 정합성 교정"
```

- [ ] **Step 5: deep-suite 마켓플레이스 등록 (릴리스 시 — 안내만)**

> 이 단계는 별도 레포(`/Users/sungmin/Dev/claude-plugins/deep-suite`) 작업이며, deep-goal이 원격에 push되고 머지 커밋 SHA가 확정된 뒤 수행한다. `deep-suite`의 `.claude-plugin/marketplace.json` + `.agents/plugins/marketplace.json`에 `deep-goal` 엔트리(40자 SHA + 한 줄 설명) 추가, `README.md`/`README.ko.md` 플러그인 표에 행 추가, Harness 매트릭스 다이어그램에 "Guides × Inferential 상위(오케스트레이션 Guide)"로 deep-goal 배치. **본 plan의 범위 밖**이며 사용자 확인 후 진행.

---

## Self-Review (작성자 체크 — 완료)

**1. Spec 커버리지:**
- §3 활성화 제약 → Task 3·6 인라인 + 진입 스킬 ✓
- §4 결정(활성화/범위/게이트/형태/시너지) → Task 3·4·5·6 ✓
- §6 디렉토리(agents/hooks 없음) → Task 2 non-goal 체크 ✓
- §8 6단계 → Task 3 ✓
- §9 rubric → Task 4 Step1 ✓
- §10 4요소·플랫폼 분기·표면화 → Task 4 Step2·3 ✓
- §11 레시피 3종 + deep-work 승인게이트 고지 → Task 5 ✓
- §12 cross-platform self-contained → Task 6 + verify 마커 ✓
- §13 비목표 → Task 2 non-goal 체크 ✓
- 릴리스/마켓플레이스 → Task 7·9 ✓

**2. Placeholder 스캔:** plan의 콘텐츠 태스크는 "담을 섹션·핵심 규칙·검증 grep"을 구체 명세(전체 산문은 산출물 자체). 코드/JSON/스크립트 스텝은 완전한 실제 내용 제공. verify-plugin.sh가 산출물의 placeholder(TBD/TODO/FIXME)를 차단.

**3. 타입/식별자 일관성:** 파일 경로·스킬명(`deep-goal`/`deep-goal-workflow`)·레시피명(robust-implementation/autonomous-evolution/ship-and-document)·검증 grep 패턴이 File Structure 표·verify 스크립트·각 태스크에서 일치.
